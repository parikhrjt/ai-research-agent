# Cloud deployment environment template (Render / Railway)
# Copy these into your platform's environment variable settings.

VECTOR_STORE=chroma
CHROMA_PERSIST_DIR=./data/chroma
LLM_PROVIDER=openai
OPENAI_API_KEY=sk-your-key-here
OPENAI_MODEL=gpt-4o-mini
PYTHONPATH=.

# Optional — set after deploying the API
API_BASE_URL=https://your-api-url.onrender.com
